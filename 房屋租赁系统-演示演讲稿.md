# 房屋租赁信息管理系统 - 演示演讲稿

**建议总时长：10-15分钟**

---

## 一、开场白（30秒）

大家好，我是XXX，来自XX班XX专业。

今天我要展示的项目是**《基于SpringBoot+Vue的房屋租赁信息管理系统》**。

这是一个帮助房东管理房源和租客寻找房屋的管理平台，主要实现两大功能：**管理端**让管理员管理用户、房屋和订单；**租客端**让普通用户浏览房屋、申请租赁和查看订单。

---

## 二、项目文件架构（2-3分钟）

在演示功能之前，我先介绍一下项目的整体架构。

项目采用**前后端分离架构**，简单来说就是后端负责处理数据，前端负责展示页面，两者分开开发，通过接口通信。

### 2.1 后端架构

**后端**基于Spring Boot，用Java语言开发，采用MVC分层架构，代码分为几个层次：

- **config配置层**：就像系统的"设置中心"，比如`SecurityConfig`负责安全配置和权限控制，`CorsConfig`处理跨域问题。

- **controller控制层**：就像"接待员"，接收前端的请求，分发给各个服务处理。比如`HouseController`处理房屋相关的请求，`LeaseOrderController`处理订单相关的请求。

- **service业务逻辑层**：就像"办事员"，处理具体的业务逻辑。比如`LeaseOrderServiceImpl`里的`applyOrder`方法，专门处理租客申请租房的逻辑。

- **entity实体层**：就像"数据表格"，每个类对应数据库的一张表，比如`House`类对应房屋表，`LeaseOrder`类对应订单表。

- **mapper数据访问层**：就像"数据库操作员"，负责和数据库打交道，执行增删改查操作。

- **utils工具类**：就像"工具箱"，放一些常用的工具方法。比如`JwtUtils`用来生成和验证登录令牌。

- **exception异常处理**：就像"急救中心"，统一处理系统运行中出现的错误，返回友好的错误信息。

### 2.2 前端架构

**前端**基于Vue.js，用JavaScript语言开发，结构如下：

- **api接口层**：就像"电话簿"，记录了所有后端接口的地址，前端通过这里调用后端。

- **views页面层**：就像"房间"，每个页面一个文件。比如`house-list/index.vue`是房屋列表页面，`my-order/index.vue`是我的订单页面。

- **router路由**：就像"导航地图"，配置了所有页面的访问路径。

- **store状态管理**：就像"储物柜"，存储用户登录信息和角色权限。

- **utils工具**：就像"快递员"，`request.js`封装了网络请求，自动携带登录令牌。

---

## 三、技术栈介绍（1分钟）

下面简单介绍一下项目使用的技术栈：

**后端**用Java语言，基于Spring Boot框架开发，用MyBatis-Plus操作数据库，用Spring Security和JWT做登录认证和权限控制。

**前端**用Vue框架和Element UI组件库做页面，用Axios和后端通信。

**数据库**用MySQL。

项目采用B/S架构，用户只要打开浏览器就能访问系统。

---

## 四、项目功能演示（5-7分钟）

### 4.1 管理员登录演示

好的，现在开始演示项目功能。首先演示登录功能。

我先用管理员账号登录，账号是admin，密码是123456。

可以看到登录成功后跳转到后台首页，左侧是菜单栏，包括数据概览、用户管理、房屋类型管理、房屋信息管理、租赁订单管理、公告管理等功能模块。顶部显示当前登录用户和退出按钮。

### 4.2 后台管理功能演示

接下来演示后台管理功能：

**第一个，用户管理**。这里可以看到所有用户列表，包括用户名、昵称、手机号、邮箱、状态等信息。管理员可以新增用户、编辑用户信息、删除用户、启用或禁用用户账号。

**第二个，房屋类型管理**。这里可以管理房屋的分类，比如"一室一厅"、"两室一厅"、"三室两厅"等。管理员可以新增类型、编辑类型名称、删除类型。

**第三个，房屋信息管理**。这里是核心功能，可以管理所有房屋信息。比如点击"新增"，可以填写房屋标题、选择类型、输入面积、户型、租金、押金、地址等信息，还能上传房屋图片。上传的图片会保存到服务器，前端可以直接访问。管理员还可以对房屋进行上下架操作，下架的房屋租客看不到。

**第四个，租赁订单管理**。这里可以查看所有订单，包括订单号、房屋信息、租客信息、租金、租期、状态等。管理员可以审核订单，点击"审核通过"，订单状态就变成"租赁中"；点击"拒绝"，订单状态就变成"已取消"。

**第五个，数据概览**。后台首页展示了关键业务指标，包括用户总数、房屋总数、订单总数、在租订单数等统计数据，还有订单趋势图和房屋类型分布图。

### 4.3 租客端功能演示

接下来切换到租客端，展示普通用户的功能：

**第一个，登录**。我用普通用户账号登录，账号是zhangsan，密码是123456。

登录成功后进入租客端首页，可以看到左侧菜单和管理员不一样，只有浏览房屋、我的订单和个人中心。

**第二个，浏览房屋**。首页以卡片式展示所有上架的房屋，每张卡片显示房屋图片、价格、标题、户型、面积、地址等信息。顶部有搜索框，可以按关键词搜索，也可以按类型和价格区间筛选。

**第三个，房屋详情**。点击任意一张房屋卡片，弹出房屋详情弹窗。可以看到房屋的多张图片轮播，以及详细的房屋信息，包括户型、面积、楼层、朝向、装修、地址、描述等。

**第四个，申请租赁**。在房屋详情页面点击"申请租赁"按钮，弹出申请表单。表单自动填充了房屋信息和月租金，用户需要选择入住日期和租期，还可以填写备注。点击"提交申请"，系统就创建了一个订单，状态是"待审核"。

**第五个，我的订单**。点击左侧菜单的"我的订单"，可以看到自己的所有订单。订单列表显示订单号、房屋信息、月租金、入住日期、结束日期、租期、状态等。可以按订单状态筛选。待审核状态的订单可以点击"取消"按钮取消申请。

**第六个，个人中心**。这里可以查看和修改个人信息，包括昵称、邮箱、手机号、头像，还可以修改登录密码。

---

## 五、结束语（30秒）

以上就是我的项目演示。

这个项目实现了房屋租赁信息管理系统的核心功能，包含了用户管理、房屋类型管理、房屋信息管理、租赁订单管理、公告管理等完整的功能模块。

系统分为租客端和管理端，支持普通用户和管理员两种角色，采用前后端分离架构，代码结构清晰，分层明确。

我的演示完毕，谢谢大家！

---

## 录制提示

1. **提前准备两个账号**：管理员（admin/123456）、普通用户（zhangsan/123456）
2. **先讲架构再演示**：先在IDEA里展示目录结构讲解，再运行项目演示功能
3. **语速适中**：不要太快，保持清晰自然
4. **边操作边讲解**：鼠标点到哪里就讲到哪里
5. **重点突出**：申请租赁和订单审核是核心功能，可以多花点时间
6. **提前准备测试数据**：确保有几条房屋数据和订单数据可以演示
7. **检查图片上传功能**：提前上传几张房屋图片，演示时效果更好
