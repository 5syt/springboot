# 《软件框架技术》课程设计报告

## 基于SpringBoot+Vue的房屋租赁信息管理系统

---

&nbsp;

&nbsp;

&nbsp;

&nbsp;

**提交日期：** 2026 年 6 月

---

## 目  录

- **1 绪论**
  - 1.1 项目背景和意义
  - 1.2 项目的目标和范围
  - 1.3 本文结构简介
- **2 相关原理和技术**
  - 2.1 IntelliJ IDEA开发工具
  - 2.2 Maven
  - 2.3 Spring Boot
  - 2.4 Vue.js
  - 2.5 MyBatis Plus
  - 2.6 MySQL数据库介绍
  - 2.7 JWT认证技术
  - 2.8 B/S体系结构
- **3 系统分析**
  - 3.1 功能需求分析
    - 3.1.1 系统租客端功能
    - 3.1.2 系统管理端功能
  - 3.2 角色用例分析
  - 3.3 系统数据流分析
  - 3.4 可行性分析
    - 3.4.1 技术可行性
    - 3.4.2 社会可行性
    - 3.4.3 操作可行性
- **4 系统总体设计**
  - 4.1 系统功能结构
  - 4.2 系统业务流程
  - 4.3 数据库设计
    - 4.3.1 概念模型设计
    - 4.3.2 物理结构设计
- **5 系统详细设计与实现**
  - 5.1 主要页面框架设计
  - 5.2 租客端功能的设计与实现
    - 5.2.1 房屋浏览页面设计
    - 5.2.2 房屋详情页面设计
    - 5.2.3 申请租赁功能设计
    - 5.2.4 我的订单页面设计
    - 5.2.5 个人中心页面设计
  - 5.3 管理端功能的设计与实现
    - 5.3.1 后台登录页面设计
    - 5.3.2 后台管理主页面设计
    - 5.3.3 用户管理模块
    - 5.3.4 房屋类型管理模块
    - 5.3.5 房屋信息管理模块
    - 5.3.6 租赁订单管理模块
    - 5.3.7 公告管理模块
- **6 系统测试**
  - 6.1 测试环境和条件
  - 6.2 测试目的
  - 6.3 测试方法
  - 6.4 测试用例
    - 6.4.1 租客端测试用例与结果
    - 6.4.2 管理端测试用例与结果
  - 6.5 测试结论
- **7 总结和展望**
- **参考文献**
- **附录**

---

## 1 绪论

### 1.1 项目背景和意义

随着我国城市化进程的不断加快，城市人口持续增长，住房租赁需求日益旺盛。传统的房屋租赁方式主要依赖于线下中介机构或个人信息发布，存在信息不对称、流程不规范、管理效率低下等问题。租客找房需要花费大量时间和精力，房东管理房屋和订单也缺乏有效的信息化手段。

在互联网技术快速发展的今天，信息化管理已经成为各行各业提升效率的重要手段。房屋租赁行业也亟需一套完善的信息管理系统，来实现房屋信息的规范化管理、租赁流程的线上化处理以及订单的跟踪管理。基于SpringBoot+Vue的房屋租赁信息管理系统，正是在这样的背景下应运而生。

本系统通过信息化手段，将房屋租赁业务从线下搬到线上，一方面为租客提供便捷的房屋浏览、在线申请租赁、订单查询等服务，另一方面为管理员提供房屋管理、订单管理、用户管理等功能，有效提升了房屋租赁业务的管理效率和用户体验，具有重要的现实意义和应用价值[1]。

### 1.2 项目的目标和范围

本项目旨在设计并实现一个基于B/S架构的房屋租赁信息管理系统，采用前后端分离的开发模式，后端使用Spring Boot框架，前端使用Vue.js框架，数据库使用MySQL。

系统的主要目标包括：
1. **房屋信息管理**：实现房屋信息的增删改查、上下架管理、分类管理等功能，管理员可以方便地维护房屋信息库。
2. **租客在线服务**：为租客提供房屋浏览、搜索筛选、查看详情、在线申请租赁、查看我的订单等功能，让租客足不出户就能完成租房申请。
3. **订单流程管理**：实现租赁订单的全生命周期管理，包括申请、审核、签约、到期、取消等状态流转。
4. **权限控制管理**：基于角色的访问控制（RBAC），区分管理员和普通用户（租客）两种角色，不同角色拥有不同的功能权限。
5. **系统数据统计**：提供数据统计仪表盘，展示系统运行情况，辅助管理员决策。

系统的适用范围包括中小型房屋租赁中介、长租公寓运营方、个人房东等，可用于管理房源和租客订单，提升租赁业务的管理效率。

### 1.3 本文结构简介

本文围绕"基于SpringBoot+Vue的房屋租赁信息管理系统"的设计与实现展开，全文共分为7章，具体内容安排如下：

**第1章 绪论**：介绍项目的研究背景、意义、目标和范围，并对本文的章节结构进行简要说明。

**第2章 相关原理和技术**：阐述系统开发过程中使用到的关键技术和工具，包括Spring Boot、Vue.js、MyBatis Plus、MySQL、JWT、Maven等，为后续的系统设计与实现提供技术理论基础。

**第3章 系统分析**：对系统进行需求分析，包括功能需求分析、角色用例分析、数据流分析，并从技术、社会、操作三个方面进行可行性分析。

**第4章 系统总体设计**：介绍系统的总体架构设计，包括系统功能结构、业务流程设计以及数据库的概念模型设计和物理结构设计。

**第5章 系统详细设计与实现**：详细介绍系统各个功能模块的具体设计与实现，包括租客端和管理端的主要页面设计和核心代码实现。

**第6章 系统测试**：介绍系统的测试环境、测试方法、测试用例以及测试结果，验证系统功能是否符合需求。

**第7章 总结和展望**：对本次课程设计的工作进行总结，分析系统的优点和不足，并对未来的改进方向进行展望。

---

## 2 相关原理和技术

### 2.1 IntelliJ IDEA开发工具

IntelliJ IDEA是JetBrains公司开发的一款Java语言集成开发环境（IDE），被广泛认为是目前最优秀的Java开发工具之一。俗话说"工欲善其事，必先利其器"，IDEA提供了丰富的功能特性，包括智能代码补全、代码重构、版本控制集成、代码分析、调试器等，能够极大地提升开发效率。

IDEA的主要优点包括：
1. **智能代码助手**：提供精准的代码补全、语法检查和快速修复建议。
2. **丰富的插件生态**：拥有庞大的插件市场，几乎支持所有主流技术栈。
3. **强大的重构功能**：支持重命名、提取方法、移动类等多种重构操作。
4. **内置版本控制**：集成Git、SVN等主流版本控制工具，操作便捷。
5. **框架支持完善**：对Spring Boot、MyBatis等主流框架有很好的原生支持。

本系统后端开发使用IntelliJ IDEA作为主要开发工具，充分利用其智能提示和调试功能，提高了代码编写和排错的效率。

### 2.2 Maven

Maven是Apache软件基金会的一个项目管理和构建自动化工具，主要用于Java项目。Maven使用项目对象模型（POM）的概念来描述项目，通过一个中央配置文件（pom.xml）来管理项目的依赖、构建、文档等。

Maven的主要优点包括：
1. **依赖管理**：自动下载和管理项目所需的jar包，解决版本冲突问题，开发者不需要手动寻找和导入依赖。
2. **项目构建**：提供统一的构建生命周期，包括编译、测试、打包、部署等阶段，一键构建项目。
3. **项目标准化**：提供标准的项目目录结构和构建流程，便于团队协作和项目维护。
4. **插件丰富**：拥有大量的插件，可以扩展各种功能，满足不同项目的需求。
5. **仓库管理**：支持本地仓库、中央仓库和私有仓库，便于依赖的管理和分发。

本系统后端项目使用Maven进行依赖管理和项目构建，通过pom.xml文件统一管理Spring Boot、MyBatis Plus、JWT等依赖，简化了项目配置和维护工作。

### 2.3 Spring Boot

Spring Boot是由Pivotal团队开发的一款基于Spring框架的快速开发脚手架，其设计目的是简化Spring应用的初始搭建和开发过程。Spring Boot遵循"约定优于配置"的理念，提供了大量的默认配置，开发者不需要再进行繁琐的XML配置，就能快速搭建一个可运行的Spring应用。

Spring Boot的主要特点和优势包括：
1. **快速开发**：内置Tomcat、Jetty等Servlet容器，项目可以直接打成jar包运行，无需部署war包。
2. **自动配置**：根据项目中的依赖自动配置Spring组件，大大减少了配置代码量。
3. **起步依赖**：提供丰富的starter依赖，只需引入一个starter就能获得一整套相关技术的支持。
4. **内嵌容器**：内置Tomcat容器，开发和部署都非常方便。
5. **监控管理**：提供Actuator组件，可以对应用进行监控和管理。
6. **生态完善**：与Spring生态的其他项目无缝集成，如Spring Security、Spring Data等。

本系统后端基于Spring Boot 2.7.18版本开发，使用Spring Security进行安全认证，使用Spring Data Redis进行缓存操作，充分利用了Spring Boot生态的便利。

### 2.4 Vue.js

Vue.js是一套用于构建用户界面的渐进式JavaScript框架，由尤雨溪于2014年发布。与其他大型框架不同的是，Vue被设计为可以自底向上逐层应用，其核心库只关注视图层，不仅易于上手，还便于与第三方库或既有项目整合。

Vue.js的主要特点包括：
1. **响应式数据绑定**：采用双向数据绑定机制，数据变化时视图自动更新，视图变化时数据也同步更新。
2. **组件化开发**：将页面拆分为独立的组件，组件可以复用、嵌套和组合，提高代码的可维护性和复用性。
3. **虚拟DOM**：通过虚拟DOM和diff算法，最小化真实DOM操作，提升页面渲染性能。
4. **指令系统**：提供v-if、v-for、v-bind、v-on等内置指令，方便操作DOM。
5. **生态丰富**：拥有Vue Router（路由管理）、Vuex（状态管理）、Element UI（组件库）等丰富的周边工具和组件库。

本系统前端基于Vue 2.7.14版本开发，使用Vue Router进行路由管理，使用Vuex进行状态管理，使用Element UI作为UI组件库，使用Axios进行HTTP请求，构建了一个功能完善的单页应用（SPA）。

### 2.5 MyBatis Plus

MyBatis Plus（简称MP）是基于MyBatis的增强工具，在MyBatis的基础上只做增强不做改变，为简化开发、提高效率而生。MyBatis Plus提供了通用的CRUD操作、分页查询、条件构造器等功能，让开发者不需要编写大量的SQL语句就能完成常见的数据库操作。

MyBatis Plus的主要特性包括：
1. **无侵入**：只做增强不做改变，引入它不会对现有工程产生影响。
2. **强大的CRUD操作**：内置通用Mapper和通用Service，通过少量配置即可实现单表的大部分CRUD操作。
3. **条件构造器**：提供LambdaQueryWrapper等条件构造器，可以灵活地构建查询条件，避免编写SQL时的字段名错误。
4. **分页插件**：内置分页插件，基于MyBatis物理分页，开发时只需简单配置即可实现分页查询。
5. **主键自动生成**：支持多种主键生成策略，包括雪花算法、UUID等。
6. **逻辑删除**：支持逻辑删除功能，删除操作变为更新操作，保护数据不被物理删除。

本系统持久层使用MyBatis Plus 3.5.3.1版本，大大简化了数据访问层的代码开发，提高了开发效率。

### 2.6 MySQL数据库介绍

MySQL是一款开源的关系型数据库管理系统，由瑞典MySQL AB公司开发，目前属于Oracle公司。MySQL是目前世界上最流行的开源数据库之一，以其体积小、速度快、成本低、开源等特点，被广泛应用于Web开发领域。

MySQL的主要优点包括：
1. **性能优异**：MySQL性能稳定，执行速度快，能够满足大多数Web应用的需求。
2. **开源免费**：MySQL采用GPL协议，可以免费使用，降低了项目成本。
3. **跨平台支持**：支持Windows、Linux、Mac OS等多种操作系统，部署灵活。
4. **易于使用**：语法简单，学习成本低，拥有丰富的文档和社区资源。
5. **可扩展性强**：支持主从复制、分库分表等扩展方案，能够应对大数据量场景。
6. **存储引擎丰富**：提供InnoDB、MyISAM等多种存储引擎，可根据业务需求选择。

本系统使用MySQL 8.x作为数据库存储，存储用户、房屋、订单等所有业务数据，使用Navicat或IDEA Database工具进行数据库管理。

### 2.7 JWT认证技术

JWT（JSON Web Token）是一种开放标准（RFC 7519），用于在各方之间安全地传输信息。JWT可以将用户信息加密到token中，服务器不需要存储session，只需要验证token的有效性即可完成用户认证，非常适合前后端分离架构和分布式系统。

JWT由三部分组成，分别是Header（头部）、Payload（载荷）和Signature（签名），三部分用点号连接形成一个完整的token字符串：
- **Header**：描述token的类型和使用的签名算法。
- **Payload**：存放用户信息和其他声明数据，如用户ID、用户名、角色等。
- **Signature**：使用密钥对前两部分进行签名，确保token不被篡改。

JWT的主要优点：
1. **无状态**：服务器不需要存储session，token包含了所有认证信息，便于水平扩展。
2. **跨域支持**：token可以放在请求头中，天然支持跨域请求。
3. **安全性高**：使用签名机制，确保token不被篡改和伪造。
4. **信息携带**：可以在payload中携带用户的基本信息，减少数据库查询。

本系统使用JWT进行用户认证授权，用户登录成功后后端生成JWT token返回给前端，前端在后续请求的请求头中携带token，后端通过过滤器验证token有效性，实现了无状态的身份认证。

### 2.8 B/S体系结构

目前主流的系统体系结构主要有C/S结构和B/S结构两种。本系统采用B/S（Browser/Server，浏览器/服务器）体系结构。

B/S结构是随着Internet技术兴起的一种网络架构模式，用户只需要在客户端安装一个浏览器（如Chrome、Firefox等），服务器端部署Web服务器和数据库，用户通过浏览器访问Web应用，与服务器进行数据交互。

B/S结构主要分为三层：
1. **表示层（浏览器端）**：用户通过浏览器访问系统，负责展示页面和与用户交互。
2. **业务逻辑层（服务器端）**：负责处理业务逻辑，接收浏览器端的请求，进行业务处理后返回结果。
3. **数据层（数据库）**：负责数据的存储和管理，为业务逻辑层提供数据支持。

B/S结构的主要优点：
1. **客户端零安装**：用户不需要安装专门的客户端软件，只要有浏览器就能使用。
2. **维护升级方便**：系统升级只需要更新服务器端代码，所有用户立即就能使用新版本。
3. **跨平台性好**：只要支持浏览器的设备都能使用，包括PC、平板、手机等。
4. **部署简单**：只需部署服务器端，降低了部署和维护成本。

本系统采用前后端分离的B/S架构，前端使用Vue.js构建单页应用，后端使用Spring Boot提供RESTful API，前后端通过HTTP协议进行JSON数据交互，具有良好的扩展性和维护性。

---

## 3 系统分析

### 3.1 功能需求分析

通过对房屋租赁业务的调研和分析，本系统主要面向两类用户：租客（普通用户）和管理员。系统分为租客端和管理端两大部分，租客端面向普通租客，提供房屋浏览、申请租赁等功能；管理端面向管理员，提供房屋管理、订单管理、用户管理等功能。

#### 3.1.1 系统租客端功能

租客端是普通用户（租客）使用的功能模块，租客需要注册登录后才能使用。租客端的主要功能包括：

1. **用户登录/注册**：租客可以通过用户名和密码登录系统，首次使用需要注册账号。
2. **房屋浏览**：租客可以浏览所有上架的房屋信息，房屋以卡片形式展示，包含图片、标题、价格、户型、面积等信息。
3. **搜索筛选**：租客可以根据关键词、房屋类型、价格区间等条件筛选房屋，快速找到符合需求的房源。
4. **房屋详情**：点击房屋卡片可以查看房屋的详细信息，包括房屋图片轮播、详细参数、房屋描述、地址等。
5. **申请租赁**：租客可以对感兴趣的房屋提交租赁申请，选择入住日期和租期，填写备注信息后提交申请。
6. **我的订单**：租客可以查看自己提交的所有租赁订单，了解订单状态（待审核、租赁中、已到期、已取消）。
7. **订单详情**：租客可以查看订单的详细信息，包括房屋信息、租金、租期、申请时间等。
8. **取消订单**：对于待审核状态的订单，租客可以主动取消申请。
9. **个人中心**：租客可以查看和修改个人信息，包括昵称、手机号、邮箱、密码等。

#### 3.1.2 系统管理端功能

管理端是管理员使用的功能模块，管理员拥有系统的全部管理权限。管理端的主要功能包括：

1. **管理员登录**：管理员通过用户名和密码登录后台管理系统。
2. **数据统计仪表盘**：首页展示系统的统计数据，包括用户总数、房屋总数、订单总数、租赁收入等，并以图表形式展示数据趋势。
3. **用户管理**：管理系统用户信息，包括新增用户、编辑用户、删除用户、禁用/启用用户、查询用户等功能。
4. **房屋类型管理**：管理房屋类型字典，包括新增类型、编辑类型、删除类型等功能。
5. **房屋信息管理**：管理房屋信息，包括新增房屋、编辑房屋、删除房屋、上下架管理、查询房屋、上传房屋图片等功能。
6. **租赁订单管理**：管理所有租赁订单，包括查看订单列表、订单详情、审核订单、更新订单状态、删除订单等功能。
7. **公告管理**：管理系统公告，包括新增公告、编辑公告、删除公告、发布/下架公告等功能。
8. **个人中心**：管理员可以修改自己的个人信息和密码。

### 3.2 角色用例分析

本系统共有两个角色，分别是租客（普通用户）和管理员。两个角色的功能权限不同，下面分别进行用例分析。

**租客用例分析**：
租客需要注册并登录系统后才能使用完整功能。租客在系统中可以浏览房屋信息、搜索筛选房屋、查看房屋详情、提交租赁申请、查看我的订单、取消待审核订单、修改个人信息等。

租客的主要用例包括：
- 登录系统
- 浏览房屋列表
- 搜索筛选房屋
- 查看房屋详情
- 提交租赁申请
- 查看我的订单列表
- 查看订单详情
- 取消待审核订单
- 修改个人信息
- 修改登录密码

**管理员用例分析**：
管理员通过后台登录系统，拥有系统的全部管理权限。管理员可以管理用户、管理房屋、管理订单、管理公告、查看数据统计等。

管理员的主要用例包括：
- 登录后台
- 查看数据统计仪表盘
- 用户管理（增删改查）
- 房屋类型管理（增删改查）
- 房屋信息管理（增删改查、上下架）
- 租赁订单管理（查看、审核、状态更新、删除）
- 公告管理（增删改查、发布/下架）
- 修改个人信息
- 修改登录密码

### 3.3 系统数据流分析

数据流图（Data Flow Diagram，简称DFD）是描述系统中数据流程的图形工具，它从数据传递和加工的角度，以图形的方式来表达数据流从输入到输出的移动变换过程。

**顶层数据流图**：
系统的外部实体是租客和管理员。租客向系统发送浏览房屋、申请租赁、查询订单等请求，系统返回房屋信息、订单信息等数据。管理员向系统发送用户管理、房屋管理、订单管理等操作请求，系统返回相应的管理数据和操作结果。

**第一层数据流图**：
将系统整体加工分解为几个主要的加工过程：用户认证、房屋信息管理、订单管理、用户管理、公告管理。租客通过用户认证后，可以进行房屋浏览、申请租赁、订单查询等操作；管理员通过用户认证后，可以进行用户管理、房屋管理、订单管理、公告管理等操作。

**第二层数据流图**：
对每个主要加工过程进一步细化。例如，订单管理可以细化为：提交订单申请、审核订单、更新订单状态、查询订单列表、查看订单详情、取消订单等加工过程，每个过程都有相应的数据输入和输出。

### 3.4 可行性分析

#### 3.4.1 技术可行性

本系统采用目前非常成熟的Java Web技术栈进行开发：
- **后端**：使用Spring Boot 2.7.18作为核心框架，Spring Security + JWT进行认证授权，MyBatis Plus作为持久层框架，MySQL 8.x作为数据库，Redis作为缓存（可选）。
- **前端**：使用Vue 2.7.14 + Vue Router + Vuex构建单页应用，Element UI作为UI组件库，Axios进行HTTP请求。
- **开发工具**：使用IntelliJ IDEA作为后端开发工具，VS Code作为前端开发工具，Maven进行项目管理，Git进行版本控制。

以上技术都是目前业界非常主流和成熟的技术，拥有丰富的文档和社区支持，遇到问题可以很方便地找到解决方案。开发人员对这些技术也有一定的掌握程度，能够熟练使用这些技术进行系统开发。因此，从技术角度来看，本系统的开发是完全可行的。

#### 3.4.2 社会可行性

随着城市化进程的加快和流动人口的增加，房屋租赁市场规模持续扩大，传统的线下租赁方式已经难以满足人们的需求。房屋租赁信息管理系统可以将租赁业务线上化，提高信息透明度，简化租赁流程，提升管理效率，无论是对租客还是对房屋管理方都有很大的价值。

从政策层面来看，国家也在大力推动住房租赁市场的发展，鼓励租赁市场的规范化和信息化。从用户层面来看，越来越多的人习惯了通过互联网找房、租房，线上租赁的接受度越来越高。因此，开发一套房屋租赁信息管理系统具有良好的社会基础和应用前景，社会可行性良好。

#### 3.4.3 操作可行性

本系统采用B/S架构，用户只需要通过浏览器就可以访问和使用系统，不需要安装额外的客户端软件，使用门槛很低。系统界面采用Element UI组件库，界面美观、操作直观，用户很容易上手。

对于租客来说，浏览房屋、申请租赁、查看订单等操作都非常简单，只需要点击按钮和填写表单即可完成，几乎没有学习成本。对于管理员来说，管理功能虽然较多，但都采用了统一的表格+表单的交互模式，操作方式一致，容易理解和掌握。因此，从操作角度来看，本系统是可行的。

---

## 4 系统总体设计

### 4.1 系统功能结构

根据需求分析的结果，系统分为租客端和管理端两大部分。系统整体功能结构如下：

**租客端功能模块**：
1. **登录注册模块**：用户登录、用户注册
2. **房屋浏览模块**：房屋列表展示、搜索筛选、房屋详情
3. **申请租赁模块**：提交租赁申请、选择入住日期和租期
4. **我的订单模块**：订单列表、订单详情、取消订单
5. **个人中心模块**：个人信息查看、个人信息修改、修改密码

**管理端功能模块**：
1. **登录模块**：管理员登录
2. **仪表盘模块**：数据统计概览、图表展示
3. **系统管理模块**：用户管理（增删改查）
4. **房屋管理模块**：
   - 房屋类型管理（增删改查）
   - 房屋信息管理（增删改查、上下架、图片上传）
5. **订单管理模块**：订单列表、订单详情、审核订单、状态更新、删除订单
6. **公告管理模块**：公告列表、新增公告、编辑公告、删除公告、发布/下架
7. **个人中心模块**：个人信息修改、修改密码

系统采用前后端分离的架构设计。前端负责页面展示和用户交互，通过HTTP请求调用后端API获取数据。后端负责业务逻辑处理和数据持久化，提供RESTful风格的API接口。前后端使用JSON格式进行数据交互。

### 4.2 系统业务流程

**租客租赁业务流程**：
1. 租客打开系统，进入登录页面
2. 输入用户名和密码登录系统（未注册则先注册账号）
3. 登录成功后进入房屋浏览页面，查看所有上架房屋
4. 租客可以通过关键词、类型、价格区间等条件筛选房屋
5. 点击房屋卡片查看房屋详情
6. 如有意向，点击"申请租赁"按钮
7. 填写入住日期、租期、备注等信息
8. 提交申请，订单状态变为"待审核"
9. 管理员审核订单，通过后状态变为"租赁中"，拒绝则变为"已取消"
10. 租客可以在"我的订单"页面查看订单状态和详情
11. 待审核状态的订单，租客可以主动取消

**管理员业务流程**：
1. 管理员通过后台登录页面登录系统
2. 登录成功后进入数据仪表盘，查看系统统计数据
3. 管理房屋信息：维护房屋类型、新增/编辑/删除/上下架房屋信息
4. 管理用户信息：查看用户列表、新增/编辑/删除用户
5. 管理租赁订单：查看所有订单、审核订单、更新订单状态
6. 管理公告：发布系统公告
7. 修改个人信息和密码

### 4.3 数据库设计

#### 4.3.1 概念模型设计

根据系统需求分析，本系统的主要实体包括：
- **用户（SysUser）**：系统用户，包括管理员和租客
- **角色（SysRole）**：用户角色，如ADMIN、USER
- **房屋类型（HouseType）**：房屋的分类，如一居室、两居室等
- **房屋（House）**：房屋的详细信息
- **租赁订单（LeaseOrder）**：租客的租赁申请和订单记录
- **公告（Announcement）**：系统公告信息
- **用户角色关联（SysUserRole）**：用户和角色的多对多关联

各实体之间的关系：
- 用户和角色是多对多关系，一个用户可以有多个角色，一个角色可以分配给多个用户
- 房屋类型和房屋是一对多关系，一个房屋类型下可以有多个房屋
- 用户（租客）和订单是一对多关系，一个租客可以有多个订单
- 房屋和订单是一对多关系，一个房屋可以有多个订单（不同时间段）

系统E-R图描述了以上实体及其之间的关系，用户实体与角色实体通过用户角色关联表建立多对多联系，房屋类型实体与房屋实体建立一对多联系，用户实体与订单实体建立一对多联系，房屋实体与订单实体建立一对多联系。

#### 4.3.2 物理结构设计

根据概念模型设计，将E-R图转换为关系数据库表。本系统数据库名为house_rental，共设计了8张数据表，分别是系统用户表、系统角色表、用户角色关联表、房屋类型表、房屋信息表、租赁订单表、公告表、系统菜单表。各表的详细结构如下：

**（1）系统用户表（sys_user）**

系统用户表用于存储管理员和租客的用户信息。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| username | varchar | 50 | NO | 用户名 |
| password | varchar | 100 | NO | 密码（加密存储） |
| nickname | varchar | 50 | YES | 昵称 |
| email | varchar | 100 | YES | 邮箱 |
| phone | varchar | 20 | YES | 手机号 |
| avatar | varchar | 255 | YES | 头像URL |
| status | tinyint | - | YES | 状态 0禁用 1启用 |
| create_time | datetime | - | YES | 创建时间 |
| update_time | datetime | - | YES | 更新时间 |
| deleted | tinyint | - | YES | 逻辑删除 0未删 1已删 |

**（2）系统角色表（sys_role）**

系统角色表用于存储角色信息。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| name | varchar | 50 | NO | 角色名称 |
| code | varchar | 50 | NO | 角色编码（如ADMIN、USER） |
| description | varchar | 255 | YES | 角色描述 |
| create_time | datetime | - | YES | 创建时间 |
| update_time | datetime | - | YES | 更新时间 |
| deleted | tinyint | - | YES | 逻辑删除 |

**（3）用户角色关联表（sys_user_role）**

用户角色关联表用于存储用户和角色的多对多关联关系。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| user_id | bigint | - | YES | 用户ID |
| role_id | bigint | - | YES | 角色ID |
| create_time | datetime | - | YES | 创建时间 |

**（4）房屋类型表（house_type）**

房屋类型表用于存储房屋的分类信息。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| name | varchar | 50 | NO | 类型名称 |
| description | varchar | 255 | YES | 描述 |
| sort | int | - | YES | 排序 |
| create_time | datetime | - | YES | 创建时间 |
| update_time | datetime | - | YES | 更新时间 |
| deleted | tinyint | - | YES | 逻辑删除 |

**（5）房屋信息表（house）**

房屋信息表用于存储房屋的详细信息。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| title | varchar | 200 | NO | 房屋标题 |
| type_id | bigint | - | YES | 房屋类型ID |
| area | decimal | 10,2 | YES | 面积（平方米） |
| rooms | varchar | 20 | YES | 户型（如 2室1厅） |
| rent_price | decimal | 10,2 | NO | 月租金 |
| deposit | decimal | 10,2 | YES | 押金 |
| address | varchar | 255 | NO | 详细地址 |
| city | varchar | 50 | YES | 城市 |
| district | varchar | 50 | YES | 区域 |
| floor | varchar | 20 | YES | 楼层 |
| orientation | varchar | 20 | YES | 朝向 |
| decoration | varchar | 20 | YES | 装修情况 |
| description | text | - | YES | 房屋描述 |
| images | text | - | YES | 图片URL列表，逗号分隔 |
| status | tinyint | - | YES | 状态 0下架 1上架 |
| create_by | bigint | - | YES | 创建人ID |
| create_time | datetime | - | YES | 创建时间 |
| update_time | datetime | - | YES | 更新时间 |
| deleted | tinyint | - | YES | 逻辑删除 |

**（6）租赁订单表（lease_order）**

租赁订单表用于存储租客的租赁申请和订单记录。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| order_no | varchar | 50 | NO | 订单编号（唯一） |
| house_id | bigint | - | YES | 房屋ID |
| tenant_id | bigint | - | YES | 租客ID |
| tenant_name | varchar | 50 | YES | 租客姓名 |
| tenant_phone | varchar | 20 | YES | 租客电话 |
| month_rent | decimal | 10,2 | YES | 月租金 |
| deposit | decimal | 10,2 | YES | 押金 |
| start_date | date | - | YES | 租赁开始日期 |
| end_date | date | - | YES | 租赁结束日期 |
| total_amount | decimal | 10,2 | YES | 总金额 |
| status | tinyint | - | YES | 状态 0待审核 1租赁中 2已到期 3已取消 |
| remark | varchar | 500 | YES | 备注 |
| create_time | datetime | - | YES | 创建时间 |
| update_time | datetime | - | YES | 更新时间 |
| deleted | tinyint | - | YES | 逻辑删除 |

**（7）公告表（announcement）**

公告表用于存储系统公告信息。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| title | varchar | 200 | NO | 公告标题 |
| content | text | - | YES | 公告内容 |
| type | tinyint | - | YES | 类型 0通知 1公告 2活动 |
| status | tinyint | - | YES | 状态 0下架 1发布 |
| create_by | bigint | - | YES | 创建人ID |
| create_time | datetime | - | YES | 创建时间 |
| update_time | datetime | - | YES | 更新时间 |
| deleted | tinyint | - | YES | 逻辑删除 |

**（8）系统菜单表（sys_menu）**

系统菜单表用于存储后台菜单信息，用于动态菜单管理。

| 列名 | 数据类型 | 字段长度 | 是否为空 | 备注 |
|------|----------|----------|----------|------|
| id | bigint | - | NO | 主键ID，自增 |
| name | varchar | 50 | YES | 菜单名称 |
| path | varchar | 255 | YES | 路由路径 |
| component | varchar | 255 | YES | 组件路径 |
| icon | varchar | 50 | YES | 图标 |
| parent_id | bigint | - | YES | 父级ID |
| sort | int | - | YES | 排序 |
| perms | varchar | 100 | YES | 权限标识 |
| type | tinyint | - | YES | 类型 0目录 1菜单 2按钮 |
| create_time | datetime | - | YES | 创建时间 |
| update_time | datetime | - | YES | 更新时间 |
| deleted | tinyint | - | YES | 逻辑删除 |

---

## 5 系统详细设计与实现

### 5.1 主要页面框架设计

本系统采用前后端分离的架构，前端使用Vue.js + Element UI构建单页应用。整体页面采用经典的后台管理系统布局，顶部为导航栏，左侧为侧边菜单，右侧为内容区域。

**页面布局结构**：
- **顶部导航栏**：显示系统logo、系统名称，右侧显示用户头像、用户名、下拉菜单（个人中心、退出登录）
- **左侧侧边栏**：根据用户角色动态显示菜单列表，管理员显示管理菜单，租客显示租客菜单
- **内容区域**：显示具体的功能页面，如仪表盘、房屋列表、订单列表等
- **底部信息**：显示版权信息等

前端项目采用模块化设计，主要目录结构如下：
- src/api：封装后端API接口请求
- src/assets：静态资源文件
- src/components：公共组件
- src/layout：布局组件
- src/router：路由配置
- src/store：Vuex状态管理
- src/utils：工具函数
- src/views：页面视图组件

### 5.2 租客端功能的设计与实现

#### 5.2.1 房屋浏览页面设计

房屋浏览页面是租客登录后的首页，以卡片式布局展示所有上架的房屋信息，视觉效果美观，浏览体验良好。

**页面功能**：
- 顶部搜索筛选区域：支持关键词搜索、房屋类型筛选、价格区间筛选
- 房屋卡片列表：每张卡片展示房屋图片、价格标签、标题、户型、面积、地址、类型标签、申请按钮
- 分页功能：支持分页浏览，可设置每页显示条数
- 空状态提示：无数据时显示友好的空状态提示

**交互流程**：
1. 页面加载时，调用后端房屋列表接口，获取上架房屋数据
2. 同时加载房屋类型列表，用于筛选
3. 用户输入关键词或选择筛选条件后，点击搜索按钮，重新加载数据
4. 点击房屋卡片，进入房屋详情弹窗
5. 点击"申请租赁"按钮，弹出申请租赁表单

**核心实现说明**：
- 使用flex布局实现响应式卡片网格，在不同屏幕尺寸下自动调整每行卡片数量
- 房屋图片为空时使用默认图片占位
- 价格标签使用绝对定位覆盖在图片左下角，视觉效果醒目
- 卡片hover时有上移动画和阴影加深效果，提升交互体验

关键代码示例（房屋列表获取）：
```javascript
fetchList() {
  const self = this
  this.loading = true
  const params = {
    keyword: this.searchForm.keyword,
    typeId: this.searchForm.typeId || undefined,
    minPrice: this.searchForm.minPrice || undefined,
    maxPrice: this.searchForm.maxPrice || undefined,
    status: 1,
    pageNum: this.pagination.pageNum,
    pageSize: this.pagination.pageSize
  }
  getHouseList(params).then(function(res) {
    const data = res.data || {}
    self.houseList = (data.records || data.list || [])
    self.pagination.total = data.total || 0
    self.loading = false
  }).catch(function(error) {
    console.error('获取房屋列表失败:', error)
    self.loading = false
  })
}
```

#### 5.2.2 房屋详情页面设计

房屋详情以弹窗形式展示，让用户无需离开列表页面就能查看房屋的完整信息。

**页面内容**：
- 图片轮播区：展示房屋的多张图片，支持自动轮播和手动切换
- 房屋标题和价格：大字体显示房屋标题和月租金
- 详细信息网格：户型、面积、楼层、房屋类型等参数
- 详细地址：显示房屋的完整地址
- 房屋描述：展示房屋的详细文字描述
- 底部操作按钮：关闭按钮、申请租赁按钮

**核心实现说明**：
- 使用Element UI的el-carousel组件实现图片轮播
- 使用el-descriptions组件展示详细信息，布局清晰
- 没有房屋图片时显示默认图片

#### 5.2.3 申请租赁功能设计

申请租赁功能是租客端的核心功能，用户在浏览房屋后可以提交租赁申请。

**申请流程**：
1. 点击"申请租赁"按钮，弹出申请表单弹窗
2. 表单自动填充房屋信息和月租金（只读展示）
3. 用户选择入住日期（不能早于今天）
4. 用户选择租期（月份数，默认12个月）
5. 用户可填写备注信息（选填）
6. 点击"提交申请"按钮，表单验证通过后调用后端接口
7. 提交成功后提示"申请提交成功，请等待管理员审核"

**后端处理逻辑**：
1. 接收申请数据，校验房屋ID、入住日期、租期等必填参数
2. 查询房屋信息，校验房屋是否存在且状态为上架
3. 自动生成订单编号（ORD+时间戳+随机数）
4. 自动设置订单状态为"待审核"（0）
5. 从当前登录用户信息中获取租客ID、姓名、电话
6. 计算结束日期 = 入住日期 + 租期月数
7. 计算总金额 = 月租金 × 租期月数
8. 保存订单数据到数据库

关键代码示例（后端申请租赁接口）：
```java
@Override
public void applyOrder(LeaseOrder leaseOrder) {
    if (leaseOrder.getHouseId() == null) {
        throw new BusinessException("房屋ID不能为空");
    }
    if (leaseOrder.getStartDate() == null) {
        throw new BusinessException("入住日期不能为空");
    }
    if (leaseOrder.getLeaseMonths() == null || leaseOrder.getLeaseMonths() < 1) {
        throw new BusinessException("租期不能为空");
    }
    House house = houseService.getById(leaseOrder.getHouseId());
    if (house == null) {
        throw new BusinessException("房屋不存在");
    }
    if (house.getStatus() == null || house.getStatus() != 1) {
        throw new BusinessException("房屋未上架，不能申请");
    }
    leaseOrder.setOrderNo(generateOrderNo());
    leaseOrder.setStatus(0);
    leaseOrder.setMonthRent(house.getRentPrice());
    leaseOrder.setDeposit(house.getDeposit());
    LocalDate endDate = leaseOrder.getStartDate().plusMonths(leaseOrder.getLeaseMonths());
    leaseOrder.setEndDate(endDate);
    if (house.getRentPrice() != null) {
        leaseOrder.setTotalAmount(house.getRentPrice().multiply(new BigDecimal(leaseOrder.getLeaseMonths())));
    }
    save(leaseOrder);
}
```

#### 5.2.4 我的订单页面设计

我的订单页面展示当前登录用户的所有租赁订单，用户可以了解自己的申请状态和订单详情。

**页面功能**：
- 搜索筛选：支持按订单号搜索、按订单状态筛选
- 订单列表：展示订单编号、房屋信息、月租金、入住日期、结束日期、租期、状态、申请时间
- 订单状态标签：待审核（橙色）、租赁中（绿色）、已到期（灰色）、已取消（红色）
- 订单详情：点击"详情"按钮查看订单完整信息
- 取消订单：待审核状态的订单可以取消
- 分页功能：支持分页浏览

**状态说明**：
- **待审核**：租客已提交申请，等待管理员审核
- **租赁中**：管理员审核通过，租赁生效中
- **已到期**：租赁期限已到
- **已取消**：申请被拒绝或租客主动取消

**后端数据查询**：
- 通过Spring Security获取当前登录用户的ID
- 查询订单表中租客ID等于当前用户ID的所有订单
- 支持按状态筛选
- 按创建时间倒序排列（最新的在前）

关键代码示例（后端我的订单接口）：
```java
@GetMapping("/my/page")
public Result<IPage<LeaseOrder>> myPage(
        @RequestParam(defaultValue = "1") Long pageNum,
        @RequestParam(defaultValue = "10") Long pageSize,
        @RequestParam(required = false) Integer status) {
    LoginUser loginUser = (LoginUser) SecurityContextHolder.getContext().getAuthentication().getPrincipal();
    SysUser sysUser = loginUser.getSysUser();
    IPage<LeaseOrder> page = leaseOrderService.pageMyOrder(pageNum, pageSize, status, sysUser.getId());
    return Result.success(page);
}
```

#### 5.2.5 个人中心页面设计

个人中心页面允许租客查看和修改自己的个人信息。

**页面功能**：
- 基本信息展示：用户名、昵称、邮箱、手机号、头像
- 修改个人信息：可以修改昵称、邮箱、手机号、头像
- 修改密码：输入旧密码和新密码，修改登录密码
- 头像上传：支持上传自定义头像

### 5.3 管理端功能的设计与实现

#### 5.3.1 后台登录页面设计

后台登录页面是管理员进入系统的入口，设计简洁美观，突出品牌感。

**页面布局**：
- 左侧：系统logo、系统名称、欢迎语
- 右侧：登录表单（用户名、密码、验证码、登录按钮）
- 底部：版权信息

**登录流程**：
1. 用户输入用户名和密码
2. 点击登录按钮，前端进行表单验证
3. 验证通过后调用后端登录接口
4. 后端验证用户名和密码，验证通过后生成JWT token
5. 前端将token保存到localStorage和Vuex中
6. 跳转到系统首页（仪表盘）
7. 后续请求在请求头中携带token

**后端认证实现**：
- 使用Spring Security + JWT实现认证
- 自定义JwtAuthenticationFilter，在请求到达Controller前验证token
- 登录成功后返回token和用户信息（包含角色信息）
- 前端根据角色信息动态渲染菜单和控制权限

关键代码示例（后端登录接口）：
```java
@PostMapping("/login")
public Result<Map<String, Object>> login(@RequestBody LoginDTO loginDTO) {
    UsernamePasswordAuthenticationToken authenticationToken = 
        new UsernamePasswordAuthenticationToken(loginDTO.getUsername(), loginDTO.getPassword());
    Authentication authentication = authenticationManager.authenticate(authenticationToken);
    LoginUser loginUser = (LoginUser) authentication.getPrincipal();
    String token = jwtUtils.createToken(loginUser.getSysUser().getId());
    Map<String, Object> data = new HashMap<>();
    data.put("token", token);
    data.put("userInfo", loginUser.getSysUser());
    data.put("roles", loginUser.getRoles());
    return Result.success(data);
}
```

#### 5.3.2 后台管理主页面设计

后台管理主页（仪表盘）是管理员登录后的首页，展示系统的整体数据统计和概览。

**页面内容**：
- 数据统计卡片：用户总数、房屋总数、订单总数、在租订单数
- 图表展示：使用ECharts绘制数据图表，如订单趋势图、房屋类型分布饼图等
- 快捷入口：常用功能的快速跳转按钮
- 最新动态：展示最新的订单、公告等信息

**后端统计接口**：
- 统计用户总数、房屋总数、订单总数、在租订单数
- 按月统计订单数量，用于折线图展示
- 按房屋类型统计房屋数量，用于饼图展示

#### 5.3.3 用户管理模块

用户管理模块用于管理系统的所有用户，包括管理员和租客。

**功能列表**：
- 用户列表展示：分页展示所有用户，显示用户名、昵称、手机号、邮箱、状态、创建时间等
- 搜索筛选：按用户名、昵称、状态等条件搜索
- 新增用户：添加新用户，设置用户名、密码、昵称、角色等信息
- 编辑用户：修改用户的基本信息
- 删除用户：删除用户（逻辑删除）
- 禁用/启用用户：切换用户的启用状态
- 重置密码：重置用户的登录密码

**前端实现**：
- 使用el-table展示用户列表
- 使用el-dialog弹窗实现新增和编辑表单
- 删除操作使用$confirm二次确认，防止误删
- 状态列使用el-switch组件，点击可切换状态

#### 5.3.4 房屋类型管理模块

房屋类型管理模块用于维护房屋的分类字典，是房屋管理的基础数据。

**功能列表**：
- 类型列表：展示所有房屋类型
- 新增类型：添加新的房屋类型
- 编辑类型：修改类型名称和描述
- 删除类型：删除房屋类型
- 排序：类型可按排序字段排序显示

**表结构**：房屋类型表包含id、name（类型名称）、description（描述）、sort（排序）等字段。

#### 5.3.5 房屋信息管理模块

房屋信息管理模块是管理端的核心功能，管理员可以维护所有房屋信息。

**功能列表**：
- 房屋列表：分页展示所有房屋，显示ID、标题、类型、面积、户型、租金、地址、状态等
- 搜索筛选：按标题、类型、状态、价格区间等条件搜索
- 新增房屋：录入房屋信息，包括标题、类型、面积、户型、租金、押金、地址、楼层、朝向、装修、描述、图片、状态等
- 编辑房屋：修改房屋的各项信息
- 删除房屋：删除房屋信息
- 上下架管理：切换房屋的上架/下架状态
- 图片上传：支持上传多张房屋图片

**图片上传实现**：
- 前端使用el-upload组件实现多图上传
- 后端接收上传的图片文件，保存到服务器本地
- 生成访问URL返回给前端
- 多张图片的URL用逗号分隔存储到数据库的images字段
- 前端展示时拆分字符串得到图片列表

关键代码示例（后端文件上传）：
```java
@PostMapping("/upload")
public Result<String> upload(@RequestParam("file") MultipartFile file) throws IOException {
    if (file.isEmpty()) {
        return Result.error("上传文件不能为空");
    }
    String originalFilename = file.getOriginalFilename();
    String suffix = originalFilename != null ? originalFilename.substring(originalFilename.lastIndexOf(".")) : "";
    String lowerSuffix = suffix.toLowerCase();
    if (!lowerSuffix.equals(".jpg") && !lowerSuffix.equals(".jpeg") && !lowerSuffix.equals(".png") && !lowerSuffix.equals(".gif")) {
        return Result.error("只支持 jpg、jpeg、png、gif 格式的图片");
    }
    String fileName = UUID.randomUUID().toString().replace("-", "") + suffix;
    String uploadDir = System.getProperty("user.dir") + File.separator + "uploads" + File.separator + "house";
    File dir = new File(uploadDir);
    if (!dir.exists()) {
        dir.mkdirs();
    }
    File destFile = new File(uploadDir + File.separator + fileName);
    file.transferTo(destFile);
    String accessUrl = "/files/house/" + fileName;
    return Result.success(accessUrl);
}
```

#### 5.3.6 租赁订单管理模块

订单管理模块用于管理所有租赁订单，管理员可以审核和处理租客的租赁申请。

**功能列表**：
- 订单列表：展示所有订单，显示订单号、房屋、租客、月租金、开始/结束日期、状态、创建时间等
- 搜索筛选：按订单号、租客姓名、状态、时间范围等条件搜索
- 订单详情：查看订单的完整信息
- 审核订单：对"待审核"状态的订单进行审核，通过则变为"租赁中"，拒绝则变为"已取消"
- 更新状态：手动更新订单状态（如标记为已到期）
- 删除订单：删除订单记录

**订单状态流转**：
1. 租客提交申请 → 待审核（0）
2. 管理员审核通过 → 租赁中（1）
3. 管理员审核拒绝 → 已取消（3）
4. 租客主动取消（待审核状态）→ 已取消（3）
5. 租赁到期 → 已到期（2）

#### 5.3.7 公告管理模块

公告管理模块用于发布和管理系统公告信息。

**功能列表**：
- 公告列表：展示所有公告，显示标题、类型、状态、创建时间等
- 新增公告：发布新公告，设置标题、内容、类型、状态
- 编辑公告：修改公告内容
- 删除公告：删除公告
- 发布/下架：切换公告的发布状态

**公告类型**：
- 通知（0）
- 公告（1）
- 活动（2）

---

## 6 系统测试

### 6.1 测试环境和条件

**硬件环境**：
- CPU：Intel Core i5及以上
- 内存：8GB及以上
- 硬盘：20GB以上可用空间

**软件环境**：
- 操作系统：Windows 10/11 或 macOS 或 Linux
- 浏览器：Chrome 90+、Firefox 88+、Edge 90+
- JDK版本：OpenJDK 11
- 数据库：MySQL 8.0.x
- 后端服务器：Spring Boot内置Tomcat
- Node.js版本：14.x及以上（前端开发环境）

### 6.2 测试目的

系统测试是软件开发过程中不可或缺的重要环节，其主要目的包括：
1. **验证功能正确性**：检查系统的各项功能是否按照需求规格说明正确实现，是否存在功能缺陷。
2. **发现软件缺陷**：通过测试尽可能多地发现系统中存在的bug和问题，以便及时修复。
3. **保证系统质量**：通过全面的测试，确保系统的稳定性、可靠性和易用性，达到交付质量标准。
4. **提升用户体验**：从用户角度测试系统的交互流程，发现并改进体验不好的地方。
5. **降低维护成本**：在开发阶段发现并修复问题，比系统上线后再修复的成本低得多。

### 6.3 测试方法

本次测试主要采用黑盒测试方法，即不考虑程序内部的逻辑结构，只检查系统功能是否按照需求规格说明正常工作。测试人员以用户的角度，通过各种输入和输出来验证系统的功能是否正确。

具体测试方法包括：
1. **功能测试**：逐项测试系统的各项功能，验证是否满足需求。
2. **界面测试**：测试系统的界面布局、样式、交互是否符合设计要求，是否美观易用。
3. **兼容性测试**：在不同的浏览器中测试系统，确保在主流浏览器中都能正常运行。
4. **边界值测试**：对输入框、分页等进行边界值测试，验证边界条件下的处理是否正确。
5. **错误推测法**：根据经验推测可能出现错误的地方，有针对性地进行测试。

### 6.4 测试用例

#### 6.4.1 租客端测试用例与结果

租客端测试用例主要覆盖租客常用的功能场景，如下表所示：

| 编号 | 测试目的 | 前提条件 | 测试步骤 | 预期结果 | 实际结果 |
|------|----------|----------|----------|----------|----------|
| 1 | 用户登录-正确账号 | 已注册用户账号 | 输入正确的用户名和密码，点击登录 | 登录成功，跳转到房屋浏览页面 | 登录成功，跳转到房屋浏览页面 |
| 2 | 用户登录-错误密码 | 已注册用户账号 | 输入正确用户名和错误密码，点击登录 | 登录失败，提示"用户名或密码错误" | 登录失败，提示"用户名或密码错误" |
| 3 | 用户登录-空输入 | - | 用户名和密码都不填，点击登录 | 提示"请输入用户名"和"请输入密码" | 提示表单验证错误 |
| 4 | 房屋列表加载 | 已登录 | 进入房屋浏览页面 | 页面正常加载，显示房屋卡片列表 | 页面正常加载，显示房屋卡片列表 |
| 5 | 房屋搜索-关键词 | 已登录，有测试房屋数据 | 在搜索框输入关键词，点击搜索 | 列表只显示包含关键词的房屋 | 列表只显示包含关键词的房屋 |
| 6 | 房屋搜索-类型筛选 | 已登录，有多种类型房屋 | 选择某个房屋类型，点击搜索 | 列表只显示该类型的房屋 | 列表只显示该类型的房屋 |
| 7 | 房屋搜索-价格区间 | 已登录 | 输入最低价格和最高价格，点击搜索 | 列表只显示价格区间内的房屋 | 列表只显示价格区间内的房屋 |
| 8 | 查看房屋详情 | 已登录，有房屋数据 | 点击房屋卡片 | 弹出房屋详情弹窗，显示完整信息 | 弹出房屋详情弹窗，显示完整信息 |
| 9 | 申请租赁-正常提交 | 已登录，有上架房屋 | 点击申请租赁，选择入住日期和租期，提交 | 提交成功，提示"申请提交成功" | 提交成功，提示"申请提交成功" |
| 10 | 申请租赁-不选日期 | 已登录 | 点击申请租赁，不选入住日期直接提交 | 提示"请选择入住日期"，不能提交 | 提示"请选择入住日期"，不能提交 |
| 11 | 我的订单-列表加载 | 已登录，有订单数据 | 点击"我的订单"菜单 | 显示当前用户的所有订单 | 显示当前用户的所有订单 |
| 12 | 我的订单-状态筛选 | 已登录，有不同状态订单 | 选择订单状态进行筛选 | 列表只显示对应状态的订单 | 列表只显示对应状态的订单 |
| 13 | 我的订单-查看详情 | 已登录，有订单数据 | 点击订单的"详情"按钮 | 弹出订单详情弹窗，信息完整 | 弹出订单详情弹窗，信息完整 |
| 14 | 我的订单-取消订单 | 已登录，有待审核订单 | 点击待审核订单的"取消"按钮，确认 | 订单状态变为"已取消" | 订单状态变为"已取消" |
| 15 | 个人中心-修改信息 | 已登录 | 修改昵称和手机号，保存 | 保存成功，信息更新 | 保存成功，信息更新 |

#### 6.4.2 管理端测试用例与结果

管理端测试用例主要覆盖管理员的各项管理功能，如下表所示：

| 编号 | 测试目的 | 前提条件 | 测试步骤 | 预期结果 | 实际结果 |
|------|----------|----------|----------|----------|----------|
| 1 | 管理员登录 | 管理员账号 | 输入管理员账号密码，点击登录 | 登录成功，进入后台仪表盘 | 登录成功，进入后台仪表盘 |
| 2 | 仪表盘数据 | 管理员已登录 | 进入仪表盘页面 | 正常显示统计数据和图表 | 正常显示统计数据和图表 |
| 3 | 用户管理-列表 | 管理员已登录 | 进入用户管理页面 | 正常显示用户列表和分页 | 正常显示用户列表和分页 |
| 4 | 用户管理-新增 | 管理员已登录 | 点击新增，填写用户信息，保存 | 新增成功，列表出现新用户 | 新增成功，列表出现新用户 |
| 5 | 用户管理-编辑 | 管理员已登录，有用户数据 | 点击编辑，修改用户信息，保存 | 修改成功，信息更新 | 修改成功，信息更新 |
| 6 | 用户管理-删除 | 管理员已登录，有用户数据 | 点击删除按钮，确认删除 | 删除成功，用户从列表消失 | 删除成功，用户从列表消失 |
| 7 | 房屋类型-新增 | 管理员已登录 | 新增一个房屋类型 | 新增成功，类型列表更新 | 新增成功，类型列表更新 |
| 8 | 房屋类型-重名 | 管理员已登录，已存在该类型 | 新增一个已存在名称的类型 | 提示类型名称重复 | 提示类型名称重复 |
| 9 | 房屋管理-新增 | 管理员已登录 | 填写房屋信息，上传图片，保存 | 新增成功，房屋列表更新 | 新增成功，房屋列表更新 |
| 10 | 房屋管理-编辑 | 管理员已登录，有房屋数据 | 编辑房屋信息，保存 | 修改成功，信息更新 | 修改成功，信息更新 |
| 11 | 房屋管理-上下架 | 管理员已登录，有房屋数据 | 点击状态开关，切换上下架 | 状态切换成功，提示上架/下架成功 | 状态切换成功，提示上架/下架成功 |
| 12 | 房屋管理-删除 | 管理员已登录，有房屋数据 | 点击删除，确认删除 | 删除成功，房屋从列表消失 | 删除成功，房屋从列表消失 |
| 13 | 订单管理-列表 | 管理员已登录，有订单数据 | 进入订单管理页面 | 显示所有订单列表和分页 | 显示所有订单列表和分页 |
| 14 | 订单管理-审核通过 | 管理员已登录，有待审核订单 | 对待审核订单执行审核通过操作 | 订单状态变为"租赁中" | 订单状态变为"租赁中" |
| 15 | 订单管理-搜索 | 管理员已登录，有订单数据 | 输入订单号搜索 | 只显示匹配的订单 | 只显示匹配的订单 |
| 16 | 公告管理-新增 | 管理员已登录 | 新增公告，填写标题和内容，发布 | 新增成功，公告列表更新 | 新增成功，公告列表更新 |
| 17 | 公告管理-删除 | 管理员已登录，有公告数据 | 删除公告 | 删除成功，公告消失 | 删除成功，公告消失 |
| 18 | 退出登录 | 管理员已登录 | 点击退出登录 | 退出成功，返回到登录页 | 退出成功，返回到登录页 |

### 6.5 测试结论

通过对系统进行全面的功能测试，测试结果如下：

1. **功能实现情况**：系统的所有主要功能都已实现，包括租客端的房屋浏览、申请租赁、我的订单、个人中心，以及管理端的用户管理、房屋管理、订单管理、公告管理等，各项功能均能正常运行，满足需求规格说明的要求。

2. **缺陷情况**：测试过程中发现了一些小问题，如个别输入框的验证提示不够友好、部分页面在特定屏幕尺寸下布局略有偏差等，这些问题都已及时修复。未发现严重的阻塞性bug。

3. **用户体验**：系统界面美观，操作流程顺畅，交互反馈及时，整体用户体验良好。租客端的卡片式房屋浏览直观易用，管理端的表格管理功能高效实用。

4. **兼容性**：在Chrome、Firefox、Edge等主流浏览器中测试，系统均能正常运行，界面显示一致，兼容性良好。

综上所述，本系统的功能基本完善，质量达到预期目标，能够满足房屋租赁信息管理的基本需求。当然，系统还有一些可以改进和优化的地方，将在后续的工作中继续完善。

---

## 7 总结和展望

### 工作总结

本次课程设计完成了一个基于SpringBoot+Vue的房屋租赁信息管理系统的设计与实现。系统采用前后端分离的B/S架构，后端使用Spring Boot + Spring Security + JWT + MyBatis Plus + MySQL技术栈，前端使用Vue.js + Vue Router + Vuex + Element UI技术栈，实现了租客端和管理端两大模块的功能。

**主要完成的工作**：

1. **系统分析与设计**：完成了系统的需求分析、可行性分析、总体架构设计、功能模块划分、数据库设计等工作，为开发奠定了基础。

2. **后端开发**：
   - 搭建了Spring Boot项目框架，配置了MyBatis Plus、Spring Security、JWT等组件
   - 实现了用户认证授权功能，基于角色的权限控制
   - 实现了用户管理、房屋类型管理、房屋管理、订单管理、公告管理等业务模块的CRUD功能
   - 实现了租客专用接口：我的订单、申请租赁
   - 实现了文件上传、数据统计等辅助功能
   - 实现了统一异常处理、统一返回结果封装

3. **前端开发**：
   - 搭建了Vue项目框架，配置了路由、状态管理、请求拦截等
   - 实现了登录页面、主布局、动态菜单、权限控制等基础功能
   - 实现了租客端：房屋浏览（卡片式）、房屋详情、申请租赁、我的订单、个人中心
   - 实现了管理端：仪表盘、用户管理、房屋类型管理、房屋管理、订单管理、公告管理
   - 实现了表单验证、列表搜索、分页、弹窗交互等通用功能

4. **系统测试**：对系统进行了全面的功能测试，验证了各功能模块的正确性，修复了发现的问题。

### 收获与体会

通过本次课程设计，我收获颇丰：

1. **技术能力提升**：对Spring Boot、Vue.js、MyBatis Plus等主流框架有了更深入的理解和实践经验，掌握了前后端分离开发的完整流程。

2. **系统设计能力**：学会了从需求分析到系统设计，再到编码实现的完整软件开发流程，提升了系统架构设计和数据库设计的能力。

3. **问题解决能力**：开发过程中遇到了很多问题，如日期格式不兼容、权限控制不生效、前端组件使用问题等，通过查阅资料和调试都一一解决，提升了独立解决问题的能力。

4. **项目管理意识**：了解了项目从规划到实现的全过程，认识到需求分析、设计规划的重要性，养成了编码前先思考设计的好习惯。

### 不足与展望

虽然系统基本功能已经实现，但由于时间和个人能力有限，系统还存在一些不足之处，有待后续改进：

1. **功能方面**：
   - 可以增加房屋收藏功能，方便租客收藏心仪的房源
   - 可以增加在线签约功能，实现电子合同签署
   - 可以增加支付功能，实现在线支付租金
   - 可以增加消息通知功能，订单状态变更时及时通知用户

2. **性能方面**：
   - 可以引入Redis缓存，缓存热点数据，提升系统响应速度
   - 可以进行数据库优化，增加索引，优化慢查询
   - 可以使用Nginx做反向代理和静态资源缓存

3. **安全性方面**：
   - 可以增加接口限流防刷机制
   - 可以增加操作日志审计
   - 可以使用更安全的密码加密算法

4. **用户体验方面**：
   - 可以增加房屋地图展示，在地图上查看房源位置
   - 可以优化移动端适配，提升手机端使用体验
   - 可以增加更多的筛选条件和排序方式

总的来说，本次课程设计是一次非常有意义的实践经历，让我将课堂上学到的理论知识真正应用到了实际项目中，受益匪浅。在今后的学习和工作中，我会继续深入学习Java Web开发相关技术，不断提升自己的技术能力和项目经验。

---

## 参考文献

[1] 基于Spring Boot的房屋租赁管理系统设计与实现[J]. 张磊, 王芳. 电脑知识与技术. 2021(20)

[2] 前后端分离架构下的Web应用研究[J]. 李明, 赵晓辉. 信息技术与信息化. 2020(05)

[3] 基于Vue.js的前端工程化实践研究[J]. 陈晨. 数字技术与应用. 2020(03)

[4] Spring Boot实战[M]. 丁雪丰 译. 人民邮电出版社, 2016.

[5] Vue.js实战[M]. 梁灏. 清华大学出版社, 2017.

[6] MyBatis从入门到精通[M]. 刘增辉. 电子工业出版社, 2016.

[7] 深入浅出MySQL[M]. 姜承尧. 人民邮电出版社, 2014.

[8] JWT(JSON Web Token)在Web认证中的应用研究[J]. 王军. 网络安全技术与应用. 2020(09)

[9] 基于RBAC的权限管理系统设计与实现[J]. 刘佳. 计算机应用与软件. 2019(06)

[10] 软件测试技术与方法研究[J]. 赵丽. 电子技术与软件工程. 2020(12)

---

## 附录

### 附录A：后端核心代码

**A.1 统一返回结果封装**
```java
package com.houserental.common;

import lombok.Data;

@Data
public class Result<T> {
    private Integer code;
    private String message;
    private T data;

    public static <T> Result<T> success() {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("操作成功");
        return result;
    }

    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("操作成功");
        result.setData(data);
        return result;
    }

    public static <T> Result<T> error(String message) {
        Result<T> result = new Result<>();
        result.setCode(500);
        result.setMessage(message);
        return result;
    }
}
```

**A.2 JWT工具类**
```java
package com.houserental.utils;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.SignatureAlgorithm;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import java.util.Date;

@Component
public class JwtUtils {

    @Value("${jwt.secret}")
    private String secret;

    @Value("${jwt.expiration}")
    private Long expiration;

    public String createToken(Long userId) {
        Date now = new Date();
        Date expireDate = new Date(now.getTime() + expiration * 1000);
        return Jwts.builder()
                .setSubject(String.valueOf(userId))
                .setIssuedAt(now)
                .setExpiration(expireDate)
                .signWith(SignatureAlgorithm.HS512, secret)
                .compact();
    }

    public Claims getClaims(String token) {
        try {
            return Jwts.parser()
                    .setSigningKey(secret)
                    .parseClaimsJws(token)
                    .getBody();
        } catch (Exception e) {
            return null;
        }
    }

    public boolean isTokenExpired(String token) {
        Claims claims = getClaims(token);
        if (claims == null) {
            return true;
        }
        return claims.getExpiration().before(new Date());
    }

    public Long getUserIdFromToken(String token) {
        Claims claims = getClaims(token);
        if (claims != null) {
            return Long.valueOf(claims.getSubject());
        }
        return null;
    }
}
```

**A.3 订单申请Service实现**
```java
@Override
public void applyOrder(LeaseOrder leaseOrder) {
    if (leaseOrder.getHouseId() == null) {
        throw new BusinessException("房屋ID不能为空");
    }
    if (leaseOrder.getStartDate() == null) {
        throw new BusinessException("入住日期不能为空");
    }
    if (leaseOrder.getLeaseMonths() == null || leaseOrder.getLeaseMonths() < 1) {
        throw new BusinessException("租期不能为空");
    }
    House house = houseService.getById(leaseOrder.getHouseId());
    if (house == null) {
        throw new BusinessException("房屋不存在");
    }
    if (house.getStatus() == null || house.getStatus() != 1) {
        throw new BusinessException("房屋未上架，不能申请");
    }
    leaseOrder.setOrderNo(generateOrderNo());
    leaseOrder.setStatus(0);
    leaseOrder.setMonthRent(house.getRentPrice());
    leaseOrder.setDeposit(house.getDeposit());
    LocalDate endDate = leaseOrder.getStartDate().plusMonths(leaseOrder.getLeaseMonths());
    leaseOrder.setEndDate(endDate);
    if (house.getRentPrice() != null) {
        leaseOrder.setTotalAmount(house.getRentPrice().multiply(new BigDecimal(leaseOrder.getLeaseMonths())));
    }
    save(leaseOrder);
}
```

### 附录B：前端核心代码

**B.1 请求拦截器配置**
```javascript
import axios from 'axios'
import { Message } from 'element-ui'
import { getToken } from '@/utils/auth'
import router from '@/router'

const service = axios.create({
  baseURL: '/api',
  timeout: 15000
})

service.interceptors.request.use(
  config => {
    const token = getToken()
    if (token && !config.url.includes('/auth/login')) {
      config.headers['Authorization'] = 'Bearer ' + token
    }
    return config
  },
  error => {
    console.error(error)
    return Promise.reject(error)
  }
)

service.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      Message.error(res.message || '请求失败')
      if (res.code === 401) {
        router.push('/login')
      }
      return Promise.reject(new Error(res.message || '请求失败'))
    } else {
      return res
    }
  },
  error => {
    console.error('请求错误:', error)
    Message.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)

export default service
```

**B.2 路由权限控制**
```javascript
router.beforeEach((to, from, next) => {
  const token = getToken()
  if (to.path === '/login') {
    next()
  } else {
    if (!token) {
      next('/login')
    } else {
      const roles = store.getters.roles || []
      if (to.meta && to.meta.roles && to.meta.roles.length > 0) {
        const hasPermission = to.meta.roles.some(role => roles.includes(role))
        if (!hasPermission) {
          next('/404')
        } else {
          next()
        }
      } else {
        next()
      }
    }
  }
})
```

---
