# 第4章 系统总体设计

## 4.1 系统功能结构

功能模块图表示目标系统应该要实现的各类功能，并将它们分类，总结。之后的设计与实现需要根据功能模块图来实现这些具体功能[5]。

房屋租赁信息管理系统分为租客端和管理端两大部分。租客端面向普通租客用户，提供房屋浏览、搜索筛选、申请租赁、订单管理、个人中心等功能；管理端面向系统管理员，提供仪表盘、用户管理、房屋管理、订单管理、公告管理、个人中心等功能。经分析系统租客端功能模块如图4-1所示：

<center>
图4-1 系统租客端功能结构图
</center>

**租客端功能结构图PlantUML代码：**

```plantuml
@startuml
!define RECTANGLE class

package "房屋租赁信息管理系统-租客端" {
  rectangle "租客端" as Root {
    rectangle "房屋浏览" as M1 {
      rectangle "房屋列表展示" as M11
      rectangle "卡片式布局" as M12
      rectangle "分页浏览" as M13
    }
    rectangle "搜索筛选" as M2 {
      rectangle "关键词搜索" as M21
      rectangle "类型筛选" as M22
      rectangle "价格区间筛选" as M23
    }
    rectangle "房屋详情" as M3 {
      rectangle "图片轮播" as M31
      rectangle "详细信息展示" as M32
      rectangle "房屋描述" as M33
    }
    rectangle "申请租赁" as M4 {
      rectangle "选择入住日期" as M41
      rectangle "选择租期" as M42
      rectangle "填写备注" as M43
      rectangle "提交申请" as M44
    }
    rectangle "我的订单" as M5 {
      rectangle "订单列表" as M51
      rectangle "订单详情" as M52
      rectangle "订单状态筛选" as M53
      rectangle "取消订单" as M54
    }
    rectangle "个人中心" as M6 {
      rectangle "修改个人信息" as M61
      rectangle "修改密码" as M62
      rectangle "头像上传" as M63
    }
  }
}

@enduml
```

系统管理端功能模块如图4-2所示：

<center>
图4-2 系统管理端功能结构图
</center>

**管理端功能结构图PlantUML代码：**

```plantuml
@startuml
!define RECTANGLE class

package "房屋租赁信息管理系统-管理端" {
  rectangle "管理端" as Root {
    rectangle "数据仪表盘" as M1 {
      rectangle "数据统计卡片" as M11
      rectangle "订单趋势图" as M12
      rectangle "房屋类型分布图" as M13
    }
    rectangle "用户管理" as M2 {
      rectangle "用户列表" as M21
      rectangle "新增用户" as M22
      rectangle "编辑用户" as M23
      rectangle "删除用户" as M24
      rectangle "启用/禁用账号" as M25
      rectangle "重置密码" as M26
    }
    rectangle "房屋类型管理" as M3 {
      rectangle "类型列表" as M31
      rectangle "新增类型" as M32
      rectangle "编辑类型" as M33
      rectangle "删除类型" as M34
    }
    rectangle "房屋信息管理" as M4 {
      rectangle "房屋列表" as M41
      rectangle "新增房屋" as M42
      rectangle "编辑房屋" as M43
      rectangle "删除房屋" as M44
      rectangle "房屋上下架" as M45
      rectangle "图片上传" as M46
    }
    rectangle "租赁订单管理" as M5 {
      rectangle "订单列表" as M51
      rectangle "订单详情" as M52
      rectangle "订单审核" as M53
      rectangle "状态更新" as M54
      rectangle "删除订单" as M55
      rectangle "订单搜索筛选" as M56
    }
    rectangle "公告管理" as M6 {
      rectangle "公告列表" as M61
      rectangle "新增公告" as M62
      rectangle "编辑公告" as M63
      rectangle "删除公告" as M64
      rectangle "发布/下架公告" as M65
    }
    rectangle "个人中心" as M7 {
      rectangle "修改个人信息" as M71
      rectangle "修改密码" as M72
    }
  }
}

@enduml
```

## 4.2 系统业务流程

经分析系统的租客端业务流程如图4-3所示：

租客进入系统后，首先需要登录账号。如果没有账号，可以先注册。登录成功后进入租客端首页，可以浏览房屋列表，使用搜索筛选功能查找合适的房屋。找到心仪的房屋后，可以查看房屋详情，了解房屋的具体信息。如果决定租赁，点击"申请租赁"按钮，填写入住日期、租期等信息后提交租赁申请。提交后订单状态为"待审核"，等待管理员审核。租客可以在"我的订单"中查看自己的订单状态和详情。如果订单还在待审核状态，租客也可以主动取消订单。管理员审核通过后，订单状态变为"租赁中"，租赁期满后订单状态变为"已到期"。

<center>
图4-3 系统租客端业务流程图
</center>

**租客端业务流程图PlantUML代码：**

```plantuml
@startuml
!theme plain
start
:进入系统;
if (是否已登录?) then (否)
  if (是否有账号?) then (否)
    :注册账号;
  endif
  :输入账号密码;
  :登录系统;
endif
:进入租客首页;
:浏览房屋列表;
:搜索筛选房屋;
:查看房屋详情;
if (是否申请租赁?) then (是)
  :选择入住日期;
  :选择租期;
  :填写备注;
  :提交租赁申请;
  :生成待审核订单;
endif
:进入我的订单;
:查看订单列表;
:查看订单详情;
if (订单状态为待审核?) then (是)
  if (是否取消订单?) then (是)
    :取消订单;
    :订单状态变为已取消;
  endif
endif
:等待管理员审核;
if (审核通过?) then (是)
  :订单状态变为租赁中;
  :租赁期满;
  :订单状态变为已到期;
else (否)
  :订单状态变为已取消;
endif
stop
@enduml
```

管理端的业务流程如图4-4所示：

管理员通过后台登录页面输入账号密码登录系统，系统验证账号密码的正确性。验证通过后进入管理端仪表盘首页，查看系统的整体运营数据统计。管理员可以根据需要进入各个管理模块进行操作：用户管理模块可以对用户进行增删改查和账号状态管理；房屋类型管理模块可以维护房屋的分类字典；房屋信息管理模块可以新增、编辑、删除房屋信息，管理房屋上下架状态，上传房屋图片；租赁订单管理模块可以查看所有订单，对待审核的订单进行审核（通过或拒绝），更新订单状态，删除订单记录；公告管理模块可以发布和管理系统公告。管理员也可以在个人中心修改自己的信息和密码。完成操作后，管理员可以退出登录。

<center>
图4-4 系统管理端业务流程图
</center>

**管理端业务流程图PlantUML代码：**

```plantuml
@startuml
!theme plain
start
:进入后台登录页面;
:输入账号密码;
:提交登录;
if (账号密码正确?) then (否)
  :返回登录页，提示错误;
  stop
endif
:登录成功，进入仪表盘;
:查看运营数据统计;
partition "用户管理" {
  :查看用户列表;
  :新增/编辑/删除用户;
  :启用/禁用账号;
  :重置用户密码;
}
partition "房屋类型管理" {
  :查看类型列表;
  :新增/编辑/删除类型;
}
partition "房屋信息管理" {
  :查看房屋列表;
  :新增/编辑/删除房屋;
  :房屋上下架管理;
  :上传房屋图片;
}
partition "租赁订单管理" {
  :查看订单列表;
  :查看订单详情;
  :审核待审核订单;
  if (审核通过?) then (是)
    :订单状态变为租赁中;
  else (否)
    :订单状态变为已取消;
  endif
  :更新订单状态;
  :删除订单;
}
partition "公告管理" {
  :查看公告列表;
  :新增/编辑/删除公告;
  :发布/下架公告;
}
partition "个人中心" {
  :修改个人信息;
  :修改登录密码;
}
:退出登录;
stop
@enduml
```

## 4.3 数据库设计

### 4.3.1 概念模型设计

经过分析本系统的实体E-R图，如图4-5所示：

系统的主要实体包括：用户（SysUser）、房屋类型（HouseType）、房屋信息（House）、租赁订单（LeaseOrder）、公告（Announcement）。

各实体之间的关系如下：
- 用户与房屋：管理员可以发布和管理多个房屋，一对多关系。
- 房屋类型与房屋：一种房屋类型对应多套房屋，一对多关系。
- 用户与租赁订单：一个租客可以有多个租赁订单，一对多关系；一套房屋可以有多个租赁订单（不同时期），一对多关系。
- 用户与公告：管理员可以发布多个公告，一对多关系。

<center>
图4-5 系统E-R模型图
</center>

**E-R模型图PlantUML代码：**

```plantuml
@startuml
!define RECTANGLE class

entity "用户 (sys_user)" as user {
  * id : BIGINT <<PK>>
  --
  username : VARCHAR(50)
  password : VARCHAR(100)
  nickname : VARCHAR(50)
  email : VARCHAR(100)
  phone : VARCHAR(20)
  avatar : VARCHAR(255)
  role : VARCHAR(20)
  status : INT
  create_time : DATETIME
  update_time : DATETIME
}

entity "房屋类型 (house_type)" as houseType {
  * id : BIGINT <<PK>>
  --
  name : VARCHAR(50)
  description : VARCHAR(255)
  sort : INT
  create_time : DATETIME
}

entity "房屋信息 (house)" as house {
  * id : BIGINT <<PK>>
  --
  title : VARCHAR(200)
  type_id : BIGINT <<FK>>
  area : DECIMAL(10,2)
  price : DECIMAL(10,2)
  deposit : DECIMAL(10,2)
  room : INT
  hall : INT
  toilet : INT
  floor : VARCHAR(20)
  orientation : VARCHAR(20)
  decoration : VARCHAR(20)
  address : VARCHAR(255)
  district : VARCHAR(50)
  images : TEXT
  description : TEXT
  status : INT
  create_by : BIGINT <<FK>>
  create_time : DATETIME
  update_time : DATETIME
}

entity "租赁订单 (lease_order)" as leaseOrder {
  * id : BIGINT <<PK>>
  --
  order_no : VARCHAR(50)
  house_id : BIGINT <<FK>>
  tenant_id : BIGINT <<FK>>
  tenant_name : VARCHAR(50)
  tenant_phone : VARCHAR(20)
  month_rent : DECIMAL(10,2)
  deposit : DECIMAL(10,2)
  start_date : DATE
  end_date : DATE
  lease_months : INT
  total_amount : DECIMAL(10,2)
  status : INT
  remark : VARCHAR(500)
  audit_time : DATETIME
  audit_by : BIGINT <<FK>>
  create_time : DATETIME
  update_time : DATETIME
}

entity "公告 (announcement)" as announcement {
  * id : BIGINT <<PK>>
  --
  title : VARCHAR(200)
  content : TEXT
  type : VARCHAR(20)
  status : INT
  create_by : BIGINT <<FK>>
  create_time : DATETIME
  update_time : DATETIME
}

houseType ||--o{ house : "一对多"
user ||--o{ house : "发布/管理"
user ||--o{ leaseOrder : "租客"
house ||--o{ leaseOrder : "租赁"
user ||--o{ announcement : "发布"

@enduml
```
